/**
* Methods for the nested
*
* @module nested
*/
(function (nested) {
	
	/**
    * Methods for the nested submodule.
    *
    * @module submodule
    */
    nested.submodule = nested.submodule || {};
})(nested = nested || {});